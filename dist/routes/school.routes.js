"use strict";
// import express , { Express, Request, Response } from "express";
// import { getSchools } from "../functions/school.functions";
// export const SchoolRouter = express.Router()
// SchoolRouter.get("/schools/:kommun", async (req: Request, res: Response) => {
//     try {
//         const schoolName = req.params.kommun;
//         const schools = await getSchools(schoolName);
//         if (schools.length === 0) {
//             res.send("No schools found").status(404);
//         }
//         res.send(schools).status(200);
//     } catch (error) {
//         res.send(error).status(500);
//     }
// });
